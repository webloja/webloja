<ul class="dropdown-menu">
    {% for sMenu in objSubMenu %}
        <li class="dropdown-submenu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown-submenu">{{sMenu.menu}}</a>
            {{ render(controller('WeblojaBundle:Menu:subMenuInterno',{'id_menu' : sMenu.id_menu })) }}
        </li>
    {% endfor %}
</ul>
